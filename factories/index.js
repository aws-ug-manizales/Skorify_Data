const UserFactory = require('./UserFactory');
const TournamentFactory = require('./TournamentFactory');
const InstanceFactory = require('./InstanceFactory');
const InstanceUserFactory = require('./InstanceUserFactory');
const MatchFactory = require('./MatchFactory');
const TeamFactory = require('./TeamFactory');

module.exports = { UserFactory, TournamentFactory, InstanceFactory, InstanceUserFactory, MatchFactory, TeamFactory };
